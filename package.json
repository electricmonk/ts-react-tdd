{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "yarn workspaces foreach -piv run dev",
    "serve:ci": "yarn workspaces foreach -piv run serve:ci",
    "test": "yarn workspaces foreach -piv run test"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@3.2.0"
}
