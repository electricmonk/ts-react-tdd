{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "yarn workspaces foreach -piv -j unlimited run dev",
    "test": "yarn workspaces foreach -piv -j unlimited run test",
    "test:ci": "yarn workspaces foreach -piv -j unlimited run test"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@3.2.0",
  "devDependencies": {
    "@types/cookie-parser": "^1.4.7",
    "cookie-parser": "^1.4.6",
    "jsdom": "^24.1.0"
  },
  "dependencies": {
    "winston": "^3.13.0"
  }
}
